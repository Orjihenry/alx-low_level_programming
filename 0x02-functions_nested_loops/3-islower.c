#include "main.h"

/**
 * int _islower -  Prints 10 times the alphabet, in lowercase
 *		followed by a new line.
 *
 * Return: 1 or 0
 */

int int _islower(void)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
		return (0);

}
